package lexicon.se;

import javax.lang.model.element.Name;
import java.lang .String;
import java.sql.SQLOutput;
import java.util.Arrays;
import java.util.Date;


public class NameRepository {

    public static void main(String[] args) {
        // addName (names,"sarah");
    }

    public static String find(final String fullName) {

        String firstName = "Ali";
        String lastName = "Mohamadi";

        System.out.println(firstName);


    }
    public static boolean add(final String fullName) {
        String[] names = new String[4];
        if (names == null) ;
        names[0] = fullName;
        return true;

        for (String name : names) {

        }
        if (fullName.equals(names)) ;
        return false;
    }

    public static void addName(String[] names, String newNames) {
        String[] newArray = Arrays.copyOf(names, names.length + 1);
        newArray[newArray.length - 1] = newNames;
        for (String name : newArray) {
            System.out.println(names);
        }

    }

    public static String[] findByFirstName(final String firstName) {
        String[] findFirstName = new String[3];

    }
    public static boolean update(final String original,final String upDateName){
        if (upDateName == null);
        return false;
         int Index= -1;
         boolean nameExists= false;
         for (int i = 0 ;i < update.Lengt; i++);

             int index=1;
             return true;

         }
    }

    }

